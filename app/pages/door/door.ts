import {Component} from '@angular/core';
import {NavController} from 'ionic-angular';

@Component({
  templateUrl: 'build/pages/door/door.html'
})
export class DoorPage {
  constructor(private navController: NavController) {
  }
}
